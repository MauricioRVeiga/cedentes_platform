<div class="row justify-content-center align-items-center min-vh-80">
    <div class="col-md-6 text-center">
        <div class="card border-0 shadow-lg">
            <div class="card-body p-5">
                <div class="mb-4">
                    <i class="fas fa-exclamation-triangle fa-5x text-warning mb-4"></i>
                    <h1 class="h2 fw-bold text-dark">Oops! Algo deu errado</h1>
                    <p class="text-muted mb-4">
                        <%= typeof message !== 'undefined' ? message : 'Desculpe, ocorreu um erro inesperado.' %>
                    </p>
                </div>

                <% if (typeof error !== 'undefined' && error.stack && process.env.NODE_ENV === 'development') { %>
                    <div class="alert alert-danger text-start">
                        <strong>Detalhes do Erro:</strong>
                        <pre class="mt-2 small"><%= error.stack %></pre>
                    </div>
                <% } %>

                <div class="d-grid gap-2 d-md-flex justify-content-md-center">
                    <a href="/dashboard" class="btn btn-primary btn-lg px-4 me-md-2">
                        <i class="fas fa-home me-2"></i>Voltar ao Dashboard
                    </a>
                    <button onclick="history.back()" class="btn btn-outline-secondary btn-lg px-4">
                        <i class="fas fa-arrow-left me-2"></i>Voltar
                    </button>
                </div>

                <div class="mt-4">
                    <small class="text-muted">
                        Se o problema persistir, entre em contato com o suporte técnico.
                    </small>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
.min-vh-80 {
    min-height: 80vh;
}
</style>